---
name: chinese-lottery-predict
description: 基于真实开奖数据为中国彩票（双色球/大乐透）生成仅供娱乐的号码推荐。当用户用中文要求基于真实数据预测、推荐或给出一组彩票号码时使用。
---

# 中文彩票号码推荐

用于基于真实开奖数据为中国彩票生成仅供娱乐的号码推荐：

- 双色球（SSQ）
- 大乐透（DLT）

彩票开奖具有随机性。此 skill 必须始终包含清晰的风险提示，且不能声称结果“准确”“必中”或有任何保证。

## 何时使用

- 用户要求基于真实开奖数据预测双色球或大乐透号码
- 用户希望根据预算得到若干组推荐号码
- 用户接受“仅供娱乐”的统计型建议，且允许联网获取真实数据

## 何时不要使用

- 用户只是询问彩票规则、玩法、开奖时间或概率原理
- 用户要求保证中奖、承诺准确率或提供带有误导性的结论
- 用户未授权联网获取真实数据
- 无法获取真实数据，但仍要求给出号码推荐

## 输入

- 彩票类型：SSQ（双色球）或 DLT（大乐透）
- 预算（可选）：例如 `10 元`、`50 元`；如果用户未提供，默认按 `10 元` 处理
- 推荐注数（可选）：默认根据预算自动推导；按 `2 元 / 组` 计算

## 输出

返回一份中文报告，包含：

- 数据来源标识（必须是真实数据）
- 热号 / 冷号摘要
- 推荐号码，并给出每组的简短理由
- 预算建议
- 风险提示

## 安全与确认

在执行联网数据获取前，先获得用户明确确认。

必须说明将按以下优先级获取真实数据：

1. `WebSearch`
2. `WebFetch`
3. `duckducksearch`
4. `tavily-search`

如果用户未授权联网，则直接停止，不允许退回模拟数据。

## 异常与停止条件

- 无法识别彩票类型时，先用一句话澄清，不要继续生成号码
- 预算不是有效整数时，要求用户重新提供
- 用户未提供预算时，默认按 `10 元` 处理，并至少输出 `5` 组号码
- 用户拒绝联网时，直接终止，不生成任何推荐号码
- 任一优先级工具不可用时，按下一优先级继续尝试
- 所有工具都无法获取真实数据时，直接报错终止，不允许使用模拟数据
- 真实数据抓取成功但关键字段缺失、号码范围错误、号码数量错误或多来源结果冲突时，直接终止，并明确说明失败原因
- 未能清晰标注数据来源 URL、抓取时间或来源名称时，不得输出推荐结果

## 工作流

1. 解析用户意图：识别彩票类型、预算，以及是否要求基于真实数据生成推荐。
2. 请求联网确认，并说明将使用的工具优先级与候选数据源。
3. 按优先级尝试获取真实数据：
   - `WebSearch`：优先搜索官方或权威开奖页面
   - `WebFetch`：抓取并解析已定位的数据页面
   - `duckducksearch`：当前两者无法拿到可用结果时作为补充搜索
   - `tavily-search`：最后兜底搜索
4. 校验真实数据：
   - 校验彩票类型、期号、开奖日期、号码数量、号码范围、号码唯一性
   - 尽可能做多来源交叉核对
   - 明确记录来源名称、URL 与抓取时间
5. 执行分析：
   - 基于选定窗口统计热号 / 冷号
   - 可选地加入简单约束，例如奇偶平衡、区间分布
6. 生成推荐：
   - 若用户未提供预算，默认按 `10 元` 处理
   - 按 `2 元 / 组` 计算推荐组数
   - 默认 `10 元` 时，至少输出 `5` 组号码
   - 至少包含一组偏热号方案和一组偏冷号方案
   - 其余方案采用混合策略
   - 推荐组数应与预算一致；如果只展示部分结果，必须明确说明
7. 输出中文报告，并附加强风险提示。

## 输出约定

输出必须是中文，并至少包含：

- 数据来源说明（来源名称、URL、抓取时间）
- 热号 / 冷号摘要
- 与预算匹配的推荐号码组数；默认 `10 元` 时至少 `5` 组
- 与预算相关的注数建议
- 明确的风险提示

## 参考资料

- 面向人的说明：[README.md](README.md)
- 数据方案与校验规则：[references/data-sources.md](references/data-sources.md)
